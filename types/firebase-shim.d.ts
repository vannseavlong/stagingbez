// shim removed - keep file present as placeholder to avoid accidental deletions
// The firebase types shim was used during development when `firebase` was not
// installed. The real `firebase` package is now installed and provides its
// own TypeScript types. Remove this file if you prefer — it is intentionally
// left blank.
